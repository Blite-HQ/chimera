# Chimera CODEOWNERS — rewritten by the S3 sanitation (2026-07-30) to match the
# real governance: decision #94 abolished per-person plano ownership. Decisions
# are made by analysis against architecture/context/system state and recorded in
# the append-only ledger (docs/mvp/decisiones.md); no person is a review gate.
#
# This file is intentionally minimal: a single catch-all so review requests
# still route somewhere, without resurrecting per-path owners. The old per-plano
# mapping lives in git history (pre-2026-07-30) and in
# docs/archivo/ratificaciones/ as historical record.
#
# When the repo goes public and external PRs exist, revisit together with the
# branch-protection plan in GOVERNANCE.md (O2/M26 pre-flip checklist).

* @dchavesh

{
  "$defs": {
    "MatchCondition": {
      "additionalProperties": false,
      "description": "What a rule applies to. Unset fields match any value.",
      "properties": {
        "side_effects": {
          "anyOf": [
            {
              "enum": ["pure", "reversible-external", "irreversible-external"],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Side Effects"
        },
        "claim_type": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Claim Type"
        }
      },
      "title": "MatchCondition",
      "type": "object"
    },
    "VerificationRule": {
      "additionalProperties": false,
      "description": "One row of the policy: for claims matching `match`, require this much.",
      "properties": {
        "match": {
          "$ref": "#/$defs/MatchCondition"
        },
        "criticality": {
          "enum": ["C0", "C1", "C2", "C3"],
          "title": "Criticality",
          "type": "string"
        },
        "min_level": {
          "enum": ["AL0", "AL1", "AL2", "AL3", "AL4"],
          "title": "Min Level",
          "type": "string"
        },
        "required_legs": {
          "default": 1,
          "minimum": 1,
          "title": "Required Legs",
          "type": "integer"
        },
        "required_anchors": {
          "default": [],
          "items": {
            "enum": ["solver", "execution", "dataset", "rule", "human"],
            "type": "string"
          },
          "title": "Required Anchors",
          "type": "array"
        },
        "escalation": {
          "anyOf": [
            {
              "const": "human",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Escalation"
        },
        "on_inconclusive": {
          "enum": ["mark", "escalate_human", "hold_run"],
          "title": "On Inconclusive",
          "type": "string"
        }
      },
      "required": ["match", "criticality", "min_level", "on_inconclusive"],
      "title": "VerificationRule",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "A versioned, auditable set of verification rules.",
  "properties": {
    "policy_id": {
      "title": "Policy Id",
      "type": "string"
    },
    "version": {
      "title": "Version",
      "type": "string"
    },
    "rules": {
      "items": {
        "$ref": "#/$defs/VerificationRule"
      },
      "title": "Rules",
      "type": "array"
    }
  },
  "required": ["policy_id", "version", "rules"],
  "title": "VerificationPolicy",
  "type": "object"
}

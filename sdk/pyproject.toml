[project]
name = "blite-capability"
version = "0.1.0"
description = "SDK: Capability port and CapabilityManifest contract for REDACTED."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/blite_capability"]

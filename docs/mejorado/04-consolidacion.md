# Mejorado — convergencia y consolidación (Etapa 3)

> **Estado: VIGENTE (2026-07-30).** Salida de la Etapa 3 del playbook: matriz
> diseño↔research, divergencias resueltas una a una, y EL backlog operativo por
> dominios — ordenado por la pregunta del criterio #101 («¿acerca esto a que un
> tercero resuelva un problema que NO es el nuestro, sin nosotros al lado y sin
> perder la confianza verificable?»). Recordatorio del mandato: la pregunta ORDENA,
> no filtra — **todo se implementa**. Decisiones #102–#105 tomadas con Dylan en esta
> sesión; resoluciones §3 ratificadas en bloque (#106). Insumos: `02-cobertura.md`,
> `03-research.md`, ledger #95–#101.

## 1 · Veredicto de convergencia: CONVERGEN

El research externo validó el diseño interno en los tres frentes (9 convergencias
independientes solo en generalidad; el patrón entry-points, el chat-como-eventos y el
stack DSSE/in-toto son estado del arte). Las únicas divergencias reales:

1. **Presupuesto de circuitos del kernel C2** (nota 02 §2.2) — supersedido en
   simulador por `FidelityStatevectorKernel`: el dataset completo es viable; la nota
   queda vigente solo para la narrativa por-circuitos/hardware.
2. **M9 nombraba a Langfuse como «backend»** — degradado a perfil OPCIONAL del
   compose (herramienta interna de debugging del proposer); el proyector OTel es lo
   que se construye. Nunca hubo decisión registrada que lo nombrara backend.
3. **Rekor** — el descarte ×2 se supersede (#105): Rekor v2 POSIX + stapled proof es
   compatible con verificación offline.

## 2 · Decisiones de la sesión de control (Etapa 3, con Dylan)

| #    | Decisión                                                                                                                                                                                                                                                                                                                                                                             | Efecto                                                                                            |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------- |
| #102 | **Freeze §13 gana supersede [MEJORADO] explícito**: el loop agéntico (ceremonia #66, `harness-agentico.md`) reemplaza la letra «pipeline fijo Fase 1» + set hardcodeado; el catálogo §14 gana los eventos de #68 (`●PlanItemUpdated`, `●ReplayDivergenceDetected`, `●ApprovalRequested/Responded`, `●MissionMessage` cuando M1 lo traiga). El freeze sigue siendo LA autoridad única | edición de `contract-freeze.md` con causa — ola 0                                                 |
| #103 | **M3 va por la ruta `formal_exact`+proof**: el puerto `RuleBackend` se diseña con la salida cvc5→Alethe→checker Carcara empaquetado (AL4 verificable offline por un tercero, freeze §4-iii literal); la v1 con Z3 emite `property_rule` AL2 honesto (+ `rlimit`, jamás timeout wall-clock). Cero techos rotos                                                                        | diseño de M3; trust/11 se traduce a clase+AL (mata `rung`)                                        |
| #104 | **Skip honesto en `GET /runs`**: la ruta de LECTURA descarta streams envenenados y expone `discarded_streams` (extensión aditiva del wire E↔D + Zod + fixture + test con la píldora #96). Línea roja: escritura/certificados/provenance siguen fail-loud                                                                                                                             | supersede de la letra «lectura hereda el fail-loud» de §3                                         |
| #105 | **Rekor re-entra** como transparency-witness OPCIONAL, pieza 5 del orden incremental de M8; supersede registrado del descarte ×2                                                                                                                                                                                                                                                     | orden M8 = hash-chain → DSSE/VSA → StatusList → OpenBao → Rekor; SPIFFE fuera (gate = multi-nodo) |

## 3 · Resoluciones de los conflictos restantes (#106 — ratificadas en bloque)

| C#             | Resolución                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C-4 (M19)      | El payload de `run.metrics.recorded` se EXTIENDE aditivo: los campos de confianza del freeze §3 se mantienen; entran `variant` (enum de 4: `quantum\|classical\|mitigated\|zne` — cubre M6) + campos científicos opcionales. Dos brazos = **sub-runs** (§13: califican — producen claims propios). Las métricas científicas que sean evidencia van ADEMÁS como deliverable con digest citado por el certificado; el evento post-terminal queda como proyección visual (fuera del hash, como está congelado) |
| C-5 (M2)       | Supersede ADITIVA de `GatewayContext` (gana `run_id`/`step_id`/`domain_id` opcionales) con la MISMA ceremonia que lo congeló. Granularidad: **un cruce por invocación de capability** (resolve es parte de mediation) — se registra como interpretación de §13 «cada step cruza». El `Pipeline` se INYECTA en `execute_run` (como `proposer`); contrato `layers` nuevo vigila la dirección gateway↛runtime                                                                                                  |
| C-6 (M4)       | El puerto gana `verify_all() -> tuple[Attestation, ...]` (default `= (verify(),)` — compat total); regla nueva explícita: **las islas de una misma corrida comparten `independence_group`** (extensión del punto 7 — jamás inflan patas); supersede con causa de la diferición registrada en decisiones-delegadas                                                                                                                                                                                           |
| C-8 (M18)      | Convención de branch-ids HÍBRIDA versionada: `edge_id_property` (FID/OBJECTID) en `geojson_to_graph` para instancias derivadas de GIS + id canónico determinista `L{min}-{max}[-k]` (k = paralela) para modelos sin GIS; versionada vía `recipe.version`+`params_digest`. Verdict por isla = `derive_execution_verdict` aplicado a los checks `island-{k}:*` de esa isla. Fixture single-origin `contract/superficie/` es precondición del merge                                                            |
| C-9 (M20)      | Supersede formal de `superficie-visual.md` §5 (la divergencia consumada se estampa); `GET /runs/{run_id}/rvsp` entra como fila nueva de `endpoints-studio.md` (clave por run; el run cita su instancia); `optimo: null` (ice-*) queda declarado fuera del endpoint                                                                                                                                                                                                                                          |
| C-10 (cr6/cr8) | Se ESTAMPA la realidad: tabla §15.3 gana los 6 digests actuales con IDs `-voltaje@v1` y nota de procedencia («doble ancla bruteforce+spectrum del espejo» — variante declarada, jamás regenerar sobreescribiendo). ieee30 (N13): misma forma — re-estampar `@v1` + attestation adicional de la 2ª ancla                                                                                                                                                                                                     |
| C-11 (M9)      | El proyector OTel es un **consumer standalone** (perfil del compose, lee el stream read-only) FUERA de `blite.*` — no toca Inv-E/INV-6; semconv pinneado y estampado. Langfuse = perfil opcional                                                                                                                                                                                                                                                                                                            |
| C-12 (M13)     | Los manifests de terceros NO son `CapabilityManifest` de primera clase: el adapter expone UN manifest genérico propio («invoke an external MCP tool») y el vocabulario del tercero entra como DATOS de configuración con digest — ADR-029 intacto por construcción. La `DispatchStrategy` de red (`service`) es parte del ítem                                                                                                                                                                              |
| C-13 (M10)     | El export Croissant declara AMBOS digests etiquetados por disciplina (digest interno del corpus + `C()` del anexo); §15.3 jamás se re-digesta. Licencia/atribución del corpus ICE se investiga y estampa ANTES de publicar                                                                                                                                                                                                                                                                                  |
| C-14 (M7/C3)   | `FormalExactPredicate` gana extensión aditiva: literal de status nuevo (`EXACT_DIAGONALIZATION`) + campo de tolerancia relativa (criterio oficial ≤5%) que entra al `verifier_params_digest`                                                                                                                                                                                                                                                                                                                |
| C-15 (M11/M20) | `baselines` sigue cerrado y se extiende COORDINADO (schema+tipo+fixture+chart en el mismo checkpoint) cuando `sa` llegue — nunca catchall silencioso                                                                                                                                                                                                                                                                                                                                                        |

## 4 · MEJORADO consolidado — backlog operativo por dominios

Cinco dominios de ejecución. El orden DENTRO de cada dominio ya aplica la pregunta;
la prioridad ENTRE dominios va en §6. Cada ítem cita su base.

### Dominio G — Generalidad (autoridad 1: los retos 2/3)

| Orden | Ítem                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Base                                        |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| G1    | **M7-núcleo reto 3**: `blite.quantum.trotter_evolve` + `blite.numeric.exact_evolve` (implementa el STUB `numeric.matrix_ops` o capability hermana) + `ExactDiagonalizationVerifier` (`formal_exact`, C-14) + corpus C3 (cadenas N∈{6,8,12} × h/J con series de ED como referencia, digests) + receta TFIM como nota KB nueva (la §3 vigente es química supersedida)                                                                                                                            | research R1 H2.1-H2.3; cobertura C M7; C-14 |
| G2    | **M7-núcleo reto 2**: `blite.ml.tabular_prep` (folds sellados por compromiso previo) + `blite.quantum.fidelity_kernel` (statevector + réplica shots, λ_min/método PSD como datos) + `blite.ml.svm_precomputed` (implementa el STUB `ml.classify`) + `blite.ml.classifier_baseline` (SVM-RBF CV-5 oficial + McNemar scipy) + `GroundTruthVerifier` + `PropertyRuleVerifier` (predicates ya congelados sin adapter) + corpus C2 sellado (CSV CC0 con digest, caveats de proveniencia declarados) | research R1 H1.1-H1.5 + §3; cobertura C M7  |
| G3    | **Dispatch por clase de problema**: `resolve_verifiers` e inputs de misión dejan de ser Reto-1-only (registro instancia→verificadores por `claim_type`; misión resuelve por clase, no por slug del corpus de islanding)                                                                                                                                                                                                                                                                        | cobertura C M7 puntos 4-5                   |
| G4    | **Policies por reto** (plantillas con techo AL3 GROUND_TRUTH para C2; criticidad/patas C3) + registro de claim_types nuevos en el perfil STEM (anexo aditivo)                                                                                                                                                                                                                                                                                                                                  | cobertura C M7 punto 6                      |
| G5    | **M11 baseline SA**: `method:"sa"` vía neal/dwave-samplers (seed pinneada, adapter de signo dimod-minimiza), ubicación en `solvers.qubo` (QUBO portable) + C-15 coordinado                                                                                                                                                                                                                                                                                                                     | cobertura C M11                             |
| G6    | **Doble ancla TFIM** (stretch): `blite.numeric.tfim_freefermion` (BdG analítico) → patrón AL4 del reto 1 replicado                                                                                                                                                                                                                                                                                                                                                                             | research R1 H2.4                            |
| G7    | **`challenges/reto2/` y `reto3/`** punta a punta (entry point único + informe por la plataforma) — la llave 1 del cierre de fase se demuestra aquí                                                                                                                                                                                                                                                                                                                                             | criterio #101                               |

### Dominio P — Producto (autoridad 2: el externo sin contexto)

| Orden | Ítem                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Base                                             |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ |
| P1    | **M32 frontera proposer**: wrap en `loop.py:610` (`plan.item_updated{failed}` ANTES del terminal, patrón #100.1) + guard de nivel task en `BackgroundTasks` + test de regresión «proposer que levanta ⇒ run.failed en el stream». Sin esto, el modo live cuelga runs — bloquea todo M1                                                                                                                                                                                                                           | cobertura A frontera; #94 desbloqueó             |
| P2    | **#104 skip honesto** en `GET /runs` + `discarded_streams` (wire aditivo + Zod + fixture + píldora #96 como test)                                                                                                                                                                                                                                                                                                                                                                                                | decisión #104                                    |
| P3    | **M1 chat real** (spec nueva `docs/specs/chat-conversacion.md`, extensión aditiva): `mission.message`/`●MissionMessage` (`POST /runs/{id}/messages`, 409 post-terminal) + `TurnContext.pending_messages` (queue-to-next-turn) + `PROMPT_PROTOCOL` v2 con historial + `run.created.thread_id?` (enhebrado post-terminal) + M21 `POST /runs/{id}/cancel` (N1) + M22 approvals punta a punta (emisor en loop + card inline + POST respuesta + Zod espejo — N2) + UI de texto libre (muere la plantilla hardcodeada) | research R2 modelo candidato; cobertura A M1-c/d |
| P4    | **M31 sesión real + replay conductor**: grabar con key de Dylan (bloqueado-por-Dylan) + `CHIMERA_MODEL_BACKEND=replay` en compose + emitir `model.call.*` (congelados y jamás emitidos) + conductor de `find_replay_divergences` (la propiedad R1 deja de ser infraestructura sin uso) + `SessionManifest` con versión y digest propio + key del modelo vía `*_FILE` + fail-fast del modo `record` efímero de la API                                                                                             | cobertura A sesión; freeze §15.7                 |
| P5    | **M27 onboarding tercero**: `scripts/generate-secrets.sh` + quickstart de 5 min que TERMINA en `verify-bundle` offline + doc de USO (la autoridad 2 no tiene artefacto hoy) + fix `docker compose up` (worker inerte documentado o perfil) + `.env.example` completo (N9) + `install-dev.sh` deja de escribir en `~/.claude` sin preguntar (N10) + init headless `CHIMERA_INIT_*` cuando P6 exista                                                                                                               | research R2 checklist; cobertura E onboarding    |
| P6    | **M15 workspaces mínimos**: `project` como fila relacional FUERA del event store + `run.created.project_id?` aditivo + selector real en el sidebar + colapsable + bloque user (organización DESCARTADA hasta 2º usuario real)                                                                                                                                                                                                                                                                                    | product-model #78; research R2 H13               |
| P7    | **M17 router real**: librería a elegir en Etapa 4 (árbol anidable `/w/:ws/p/:proj/runs/:id/:tab` desde el día 1 — letra #78), `Tabs` controlado, go-back en Artifacts/Papers/Knowledge, SPA fallback en nginx verificado                                                                                                                                                                                                                                                                                         | cobertura D M17                                  |
| P8    | **M16 branding**: ingresar las 21 referencias al repo (precondición), decidir red-de-nodos vs 3-barras (la colisión glifo↔logomark del #99.1 empuja a red-de-nodos), sistema de marca (16px, claro/oscuro, lockups) + supersede de DESIGN.md §7/§4                                                                                                                                                                                                                                                               | cobertura D M16                                  |
| P9    | **M14 distribution root**: `distributions/chimera/pyproject.toml` con extras curados + Dockerfile `--package` + guard: `policy_digest` byte-idéntico + smoke 2.5 verde + meta de tamaño de imagen                                                                                                                                                                                                                                                                                                                | cobertura D M14                                  |
| P10   | **M24 Papers/upload** (N5): endpoint de archivos + listado (diseño completo — reusa capability ingesta)                                                                                                                                                                                                                                                                                                                                                                                                          | cobertura D honest-empty #8                      |

### Dominio C — Confianza (autoridad 3: el freeze/certificado)

| Orden | Ítem                                                                                                                                                                                                                                                                                                                                                                             | Base                                         |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- |
| C1    | **Manifest v2 (4 campos §1)** — el desbloqueador: SDK gana `side_effects`/`required_permission`/`interaction`/`execution_profile` (congelados con tabla; SQL y matriz dispatch ya listos); migración coordinada de las 13 capabilities + gate de genericidad extendido                                                                                                           | cobertura B M8; «mayor ratio valor/esfuerzo» |
| C2    | **M2 cruce por step + AX1**: supersede aditiva de `GatewayContext` (C-5) + 6 etapas reales (identity/authz/guardrails/provenance×2/verification) + inyección del Pipeline + mapeo `Rejection`→eventos + JWT en cookie (decidido §9 P1-9, sesión de seguridad del API) + `IdentityStage` estampa → el xfail AX1 se voltea (ruta congelada §134, jamás antes) + `_API_ACTOR` muere | cobertura A M2; C-5                          |
| C3    | **M3 RuleVerifier** por #103: `RuleBackend` (Z3 con `rlimit`; diseño con salida de proof para cvc5/Alethe/Carcara) + `RuleSet` como datos versionados con digest + traducción de trust/11 a clase+AL + registro en trust-registry                                                                                                                                                | decisión #103; cobertura B M3                |
| C4    | **M4 attestation por isla** por C-6: `verify_all()` + `step_id = island_id` estable (depende de la convención C-8) + verdict per-isla + regla de `independence_group` compartido + punto de checklist de coherencia + badges nativos en la ruta live                                                                                                                             | cobertura B M4; C-6                          |
| C5    | **M8 pieza 1 — hash-chain**: cómputo en el writer único (corte [stress-final] respetado: head = evento terminal) + verificación + punto de checklist; **M28**: reconciliación `sub_run_provenance_hash` (constante/vista/canonicalización) + punto 9 del checklist (recompute del sub-run que el anexo CONGELADO ya manda) + stream del sub-run empaquetado en el bundle         | cobertura B M8/sub_run; research R3          |
| C6    | **M8 pieza 2 — DSSE por attestation** (predicate sobre VSA/SVR) + supersede del punto 7 + habilita M4 «de primera clase»                                                                                                                                                                                                                                                         | research R3 §1; cobertura B                  |
| C7    | **M8 pieza 3 — StatusList** (forma Bitstring, artefacto estático firmado, `verify-bundle --status-list` opcional) + supersede de «una emisión por run» + `●CertificateRevoked` tipado                                                                                                                                                                                            | research R3 §4                               |
| C8    | **M8 pieza 4 — OpenBao Transit** single-node: cablear `assemble`/`dsse` al puerto `KeyProvider` (hoy no lo usan) + adapter Transit + unseal automatizado en compose                                                                                                                                                                                                              | research R3 §6; cobertura B                  |
| C9    | **M8 pieza 5 — Rekor v2 witness** opcional (#105): `rekor-server-posix` en perfil + stapled proofs en el bundle                                                                                                                                                                                                                                                                  | decisión #105                                |
| C10   | **M29 OverrideEvent** (§10 completo → código): `OverridePayload` Pydantic + chequeo `override:apply:<scope>` (match EXACTO — se estampa por escrito, A6 ya lo asume) + Stage emite antes de aplicar (INV-4) + test AX2 «desactivar el registro es un override»                                                                                                                   | cobertura B OverrideEvent                    |
| C11   | **M8 resto declarado**: MCP de salida (forma del adapter), ingesta KG (doc Fase 2 ya existe), Fargate/BYOC (IaC) — al final del dominio, tras el flip OSS si llega                                                                                                                                                                                                               | cobertura B/E                                |

### Dominio V — Visual/ciencia (productores que faltan)

| Orden | Ítem                                                                                                                                                                                                                                                                                                                                                  | Base                     |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ |
| V1    | **M18 partición sobre el mapa** por C-8: convención híbrida de branch-ids + agregación per-isla + productor (`verification.completed.partition` ratificada como llave) + `step_id` top-level (**M23a**/N3 — mata `attestations: []`) + fixture `contract/superficie/` + `getTopology`/query/Zod + render del overlay con reconciliación honesta 68/70 | cobertura D M18; C-8     |
| V2    | **M19 ablación** por C-4: payload extendido + dos brazos como sub-runs + instrumentar `verification_latency_ms` + puente ciencia→stream (el JSON de `results/` entra por eventos con digest)                                                                                                                                                          | cobertura C M19; C-4     |
| V3    | **M20 rvsp** por C-9: ángulos de Nexus ingeridos con digest (extensión del importador) + `optimize:false`/`initial_angles` en QAOA (⟨C⟩ en ángulos DADOS — `expected_energy` ya existe) + decisión de curva (Aer multi-semilla etiquetado para std/min/max) + endpoint + supersede §5                                                                 | cobertura C M20; C-9     |
| V4    | **M6 ZNE**: capability propia (~50 líneas folding+extrapolación) + control negativo garbage-folding como test de primera clase + bloque `mitigation.*` emitido (congelado en §11, cero código hoy) + panel de 4 barras (enum C-4) + Mitiq SOLO dev/benchmarks (GPL)                                                                                   | cobertura C M6           |
| V5    | **M6 warm-start/INTERP**: `initial_angles`/`warm_start` aditivo en QAOA                                                                                                                                                                                                                                                                               | cobertura C M6           |
| V6    | **M5 qnexus vivo**: adapter cliente (submit/poll/normalizar) bajo el gateway con `side_effects` correcto (§13 no-retry, escala a humano — usa M22) + egress policy + separación corrida-viva/artefacto-congelado + ruta de transpilación real (digests de bytes reales)                                                                               | cobertura C M5           |
| V7    | **M6 QEC/Iceberg** (último: el enunciado advierte degradación; el entregable es el tradeoff medido — depende de V6)                                                                                                                                                                                                                                   | cobertura C M6           |
| V8    | **M23b** (N4): `deliverables=` a `assemble_bundle` + rutas de nivel proyecto para Artifacts/Knowledge                                                                                                                                                                                                                                                 | cobertura D honest-empty |

### Dominio O — Observabilidad/publicación

| Orden | Ítem                                                                                                                                                                                                                                                                                                       | Base                      |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- |
| O1    | **M25 CI/repo** (N6/N8/N12): arreglar el gate de docs (excluir/desvendorizar el árbol quantathon — 671 errores hoy; CUALQUIER PR con .md falla) + CI en ramas de trabajo + CODEOWNERS alineado a #94 + reconciliar `MODEL_ROUTER_BACKEND`↔`CHIMERA_MODEL_BACKEND` en el freeze                             | cobertura E CI            |
| O2    | **M26 pre-flip OSS** (N7/N11): la regla de marca interna pasa a enforcement VERSIONADO que viaja con el repo (candidato: regla en pre-commit/semgrep — se diseña sin delatar el patrón) + sanitización del árbol vendorizado de terceros + gitleaks local en pre-commit + DCO bot cuando haya PRs externos | cobertura E OSS           |
| O3    | **M9 proyector OTel** por C-11: consumer standalone en perfil compose + spec de mapeo evento→span (trace-id determinista para que el replay dé trazas idénticas) + Langfuse como perfil opcional                                                                                                           | cobertura E M9; C-11      |
| O4    | **M10 Croissant** por C-13: mapeo instancia→JSON-LD + dualidad de digests declarada + licencia ICE investigada + ruta de catálogo de instancias en el api                                                                                                                                                  | cobertura E M10; C-13     |
| O5    | **M13 MCP-cliente** por C-12: manifest genérico envolvente + `DistributionManifest` materializado (allowlist de servers + egress + pins) + `DispatchStrategy` de red + attestation de importación (reusa evidencia-externa; `builder.id = mcp://<server>/<tool>`) — `qnexus-mcp` como primer caso          | cobertura E M13; C-12     |
| O6    | **M30** por C-10: estampar §15.3 (cr6/cr8 `-voltaje@v1` + procedencia) + identidad ieee30 + análisis de flips §15.5 repetido para cr6/cr8                                                                                                                                                                  | cobertura C cr6/cr8; C-10 |
| O7    | **M12 deck.gl**: CONDICIONAL con umbral definido (features>5k o FPS<30 en el mapa con overlay) — y solo tras Fase 2 MapLibre o su descarte formal                                                                                                                                                          | cobertura D M12           |

## 5 · Orden global entre dominios (la pregunta aplicada)

1. **G (generalidad)** — la autoridad 1 responde primero: sin retos 2/3 corriendo no
   hay «problema que NO es el nuestro».
2. **P (producto)** — P1/P2 son prerequisito de cualquier tercero tocando el sistema;
   P3-P5 son «sin nosotros al lado».
3. **C (confianza)** — C1 (manifest v2) va temprano porque desbloquea G3, O5 y C2;
   el resto profundiza el diferenciador sin bloquear a nadie.
4. **V (visual/ciencia)** — el payoff visible; V1-V3 cierran los honest-empty.
5. **O (plataforma)** — O1 es quick-win de ola 0 (el gate roto estorba TODO); el
   resto acompaña.

**Ola 0 (sesión de control, inmediata)**: #102 supersede §13 + O1 (gate de docs) +
O6/M30 (estampados documentales) + supersede de `superficie-visual.md` §5 (C-9) +
degradación de M9 en el enunciado. Todo docs/config — sin tocar el runtime.

## 6 · Fuera de alcance reafirmado

- SPIFFE/SPIRE: gate por despliegue multi-nodo, NO por fase (#105 ámbito).
- Organización como entidad: hasta el segundo usuario real (research R2 H13).
- deck.gl: condicional con umbral (O7) — no es compromiso.
- La fase siguiente parqueada (3 ideas ganadoras, comparación vs hackathon, research
  de competencia) — registrada en `01-criterio.md`, NO entra aquí.

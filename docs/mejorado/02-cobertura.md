# Mejorado — cobertura del diseño (Etapa 1)

> **Estado: VIGENTE (2026-07-30).** Salida de la Etapa 1 del playbook. Cinco
> exploraciones paralelas solo-lectura sobre `planeado/base` @ `8d0620f` mapearon el
> backlog M1–M20 + la deuda heredada contra lo YA diseñado/construido. Convención:
> **DISEÑADO-DECIDIDO** (decisión cerrada con forma escrita) · **SEMILLA** (código/
> campo/test parcial reutilizable) · **SOLO-MENCIONADO** (nombrado sin forma) ·
> **AUSENTE**. Cada fila cita su referencia; los conflictos de contrato van al final
> (alimentan la discusión de la Etapa 3).

## 1 · Matriz maestra

| Ítem                                      | Clasificación                                                                                                                                                                      | Bloqueo #1 concreto                                                                                                                                               |
| ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M1-a flip proposer (replay\|record\|live) | DISEÑADO-DECIDIDO, implementado verde                                                                                                                                              | —                                                                                                                                                                 |
| M1-b LLM vivo como default                | SEMILLA                                                                                                                                                                            | sin sesión grabada ni key `*_FILE`; supersede de freeze §15.4 (lista NO-va) con causa                                                                             |
| M1-c contexto entre turnos                | SEMILLA                                                                                                                                                                            | `TurnContext` no porta historial (`loop.py:159-175`); `PROMPT_PROTOCOL` v1→v2                                                                                     |
| M1-d chat multi-turno LIBRE               | **AUSENTE**                                                                                                                                                                        | cero endpoint de mensajes, cero entidad, cero evento `●Message*`, UI = dos `Select` sin texto libre                                                               |
| M2 cruce del gateway por step             | SEMILLA                                                                                                                                                                            | `GatewayContext` CONGELADO sin `run_id`/`step_id`; 6 de 8 etapas inexistentes; contradicción §13 por-step ↔ `MediationStage` (resolve+invoke en una etapa)        |
| M2 flip AX1                               | SEMILLA (xpass verificado en vivo)                                                                                                                                                 | no existe `IdentityStage` ni JWT; la ruta congelada §134 prohíbe voltear el xfail antes                                                                           |
| M3 Z3 RuleVerifier                        | DISEÑADO-DECIDIDO (spec trust/11) + SEMILLA (campos del predicate + stub `capabilities/smt`)                                                                                       | trust/11 escrito en vocabulario MUERTO (`rung`); techo AL2 de `property_rule` (ver conflicto C-2)                                                                 |
| M4 attestation por isla                   | DISEÑADO-DECIDIDO (trust/12 + diferición registrada con causa) + SEMILLA                                                                                                           | `Verifier.verify() -> Attestation` singular; `independence_group` por isla = línea roja (inflar patas del punto 7)                                                |
| M5 pata Guppy/qnexus viva                 | SEMILLA (importador 3-capas completo + Guppy smoke)                                                                                                                                | cliente `qnexus` vivo fuera de toda spec; seed del emulador H2 sin pinning; job Nexus = efecto externo con costo (§13 no-retry)                                   |
| M6 ZNE / warm-start / QEC                 | ZNE DISEÑADO-DECIDIDO (~50 líneas propias, Mitiq solo dev) · warm-start SOLO-MENCIONADO (INTERP no implementado) · QEC/Iceberg SOLO-MENCIONADO (el enunciado advierte degradación) | bloque `mitigation.*` congelado en §11 y CERO código lo emite; panel de 4 barras choca con `variant` de 2 valores                                                 |
| M7 retos 2/3                              | **SOLO-MENCIONADO** (ciencia C2 al detalle en KB; receta C3 NO escrita — supersede S-E)                                                                                            | 4 capabilities + 2-3 verificadores nuevos; `resolve_verifiers` e inputs de misión son Reto-1-only; corpus 100% grafo                                              |
| M8 hash-chain                             | DISEÑADO-DECIDIDO + SEMILLA (la más avanzada: fórmula/génesis/prefijo congelados, columnas vacías desde la semilla)                                                                | cómputo en el writer + verificación + punto de checklist                                                                                                          |
| M8 DSSE por attestation                   | DISEÑADO-DECIDIDO + SEMILLA fuerte (`dsse.py` sirve tal cual)                                                                                                                      | dónde viven los envelopes en el bundle + supersede del punto 7                                                                                                    |
| M8 StatusList                             | SOLO-MENCIONADO (hueco tipado: `revocation: Literal["none"]`)                                                                                                                      | elegir forma (research: Bitstring como artefacto estático) + superseder «una emisión por run»                                                                     |
| M8 OpenBao/HSM                            | DISEÑADO-DECIDIDO (escalera de 3 escalones como contrato) + SEMILLA (Protocol `KeyProvider` sin adapters; `assemble()` NO usa el puerto)                                           | cablear assemble/dsse al puerto + adapter Transit                                                                                                                 |
| M8 SPIFFE                                 | forma DISEÑADA + campo `spiffe_id` reservado                                                                                                                                       | research: DESCARTAR salvo multi-nodo (gate por despliegue)                                                                                                        |
| M8 Rekor                                  | SOLO-MENCIONADO con **descarte activo registrado ×2**                                                                                                                              | re-litigar (research: Rekor v2 posix + stapled proof ya no rompe air-gap)                                                                                         |
| M8 Fargate/BYOC                           | DISEÑADO-DECIDIDO (deployment.md) + AUSENTE en código (cero IaC)                                                                                                                   | — (post lo demás)                                                                                                                                                 |
| M8 MCP de salida                          | SOLO-MENCIONADO (dep elegida y pinneada v1.x, licencia verificada)                                                                                                                 | forma del adapter sin escribir                                                                                                                                    |
| M8 ingesta KG                             | DISEÑADO-DECIDIDO (doc propio Fase 2 + frontera RAG-jamás-evidencia congelada) + SEMILLA fuerte (provenance + capability ingesta)                                                  | —                                                                                                                                                                 |
| M8 manifest v2 (4 campos §1)              | DISEÑADO-DECIDIDO (congelado con tabla) + SEMILLA (SQL con CHECK + matriz dispatch testeada; **el SDK sigue en v1**)                                                               | **mayor ratio valor/esfuerzo de M8** — desbloquea loop.py, stages, ingesta, M13                                                                                   |
| M9 proyector OTel                         | SEMILLA (3 líneas de diseño en 03-research Planeado)                                                                                                                               | spec inexistente; dónde vive el exportador (conflicto Inv-E/INV-6)                                                                                                |
| M9 Langfuse                               | SOLO-MENCIONADO (la frase de M9 es su única aparición como «backend»; trust/18 lo usa solo como inspiración UX)                                                                    | research: degradar a perfil opcional                                                                                                                              |
| M10 Croissant                             | SOLO-MENCIONADO («sale casi gratis», sin justificar) + sustrato SEMILLA (corpus 16 archivos con digest + provenance)                                                               | dos disciplinas de digest coexisten; corpus SIN licencia; cero ruta de instancias en el api                                                                       |
| M11 baseline SA                           | SEMILLA (neal probado vivo en smoke; extra `dwave` declarado sin implementación)                                                                                                   | ubicación (maxcut `method:"sa"` vs solvers) + `baselines` cerrado a 3 claves en el frontend + signo (dimod minimiza)                                              |
| M12 mapa deck.gl                          | SOLO-MENCIONADO con descarte razonado previo                                                                                                                                       | sin umbral definido; Fase 2 (MapLibre+PMTiles) no construida — deck.gl sería el 3er peldaño                                                                       |
| M13 adapter MCP-cliente                   | SEMILLA (mapeo manifest↔tool congelado… en sentido SALIDA)                                                                                                                         | `DistributionManifest` no existe como código; `DispatchStrategy` de red no existe; ADR-029 vs manifests importados (filoso)                                       |
| M14 distribution root                     | SOLO-MENCIONADO                                                                                                                                                                    | `distributions/chimera/pyproject.toml` nuevo + extras curados; riesgo: `policy_digest` = bytes exactos del YAML tal como se distribuye                            |
| M15 sidebar completo                      | modelo DISEÑADO-DECIDIDO (#78, `docs/studio/product-model.md` — ver corrección abajo) · UI SEMILLA (pill sin dropdown) · user/org **AUSENTE**                                      | cero endpoint/tipo `Workspace`/`Project`; sin `GET /me`                                                                                                           |
| M16 branding/logo                         | SEMILLA (logomark 3-barras + favicon existen; DESIGN.md define identidad completa)                                                                                                 | las 21 referencias de Dylan NO están en el repo; colisión glifo(3 barras #99.1)↔logomark(geometría F1 «fija»)                                                     |
| M17 URLs reales                           | regla de rutas DISEÑADA-DECIDIDA (#78: anidables `/w/:ws/p/:proj/...`) · router **AUSENTE** (navegación = `useState`)                                                              | elegir librería (sin precedente); `Tabs` no controlado; nginx SPA fallback a verificar                                                                            |
| M18 partición sobre el mapa               | SEMILLA (payload 4-campos congelado; seam `partition?` declarado sin render; honest-empty vivo)                                                                                    | convención VERSIONADA de branch-ids AUSENTE **+ 4º hueco: `verification` POR isla vs attestation global**                                                         |
| M19 ablación `run.metrics.recorded`       | SEMILLA consumidor (ruta+Zod+panel verdes) · productor **AUSENTE**                                                                                                                 | **CHOQUE REAL**: freeze §3 define campos de confianza; el consumidor espera campos científicos por variante — un mismo tipo de evento, dos payloads incompatibles |
| M20 `GET /rvsp`                           | SEMILLA consumidor (schema D5 solo en frontend) · productor+ciencia **AUSENTES**                                                                                                   | el endpoint no está en NINGUNA spec; ⟨C⟩ exige evaluar en los ángulos de Nexus — que NO están en Chimera (viven en el espejo `reto1-vanilla`)                     |

## 2 · Deuda heredada (estado exacto)

| Deuda                                                | Clasificación                                                                                            | Residuo real                                                                                                                                                                                                                                                                                                                                                                                         |
| ---------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Sesión agéntica real grabada                         | mecanismo DISEÑADO-DECIDIDO punta a punta · artefacto **AUSENTE**                                        | `knowledge/sessions/` NO existe; correr `scripts/record_session.py` con key (bloqueado-por-Dylan); además: la API en modo `record` NO persiste (trampa operativa sin guard), `SessionManifest` sin digest propio ni versión, y `model.call.*` **congelados en §3 y jamás emitidos** — `find_replay_divergences` no tiene ningún caller productivo (la propiedad R1 es infraestructura sin conductor) |
| Frontera proposer en `loop.py`                       | frontera DISEÑADA y localizada (`loop.py:610-620`, sin try/except) · fix **AUSENTE**                     | gobernanza #94 ya desbloqueó la propiedad del archivo; el tuple del centinela NO cubre fallas live (Timeout/RateLimit escapan crudas ⇒ run colgado sin terminal — exactamente el modo que M1-b quiere volver default); `BackgroundTasks` sin red de seguridad; cero test de regresión                                                                                                                |
| Punto 4 auditoría — fail-loud vs `discarded_streams` | doctrina vigente DISEÑADA (fail-loud «contrato, no cortesía») · cambio SOLO-MENCIONADO y **SIN DECIDIR** | precedente interno a favor del skip honesto EN EL MISMO `reads.py` (ablación omite por-fila; topología descarta por-unidad); la tolerancia sería SOLO de la ruta de lectura — el camino que alimenta certificados no se toca (línea roja)                                                                                                                                                            |
| Ex-PENDIENTE #66 (supersede A1)                      | IMPLEMENTADO verde; ratificación DISUELTA por #94                                                        | **el residuo real es C-1**: freeze §13 sigue afirmando el pipeline fijo mientras el código corre el loop agéntico — la regla del propio índice de specs («una spec jamás contradice el freeze») está siendo violada por `harness-agentico.md`; catálogo §14 sin los eventos de #68                                                                                                                   |
| `sub_run_provenance_hash`                            | SEMILLA implementada con divergencia de fórmula RECONOCIDA                                               | dos fórmulas (prefijo/vista/canonicalización distintos); el anexo CONGELADO manda un recompute del sub-run que `verify-bundle` NO hace (grep: cero hits) — letra muerta; el stream del sub-run ni siquiera se empaqueta en el bundle                                                                                                                                                                 |
| `OverrideEvent`/`OverridePayload` (§10)              | DISEÑADO-DECIDIDO completo · **AUSENTE en código** (grep confirmado ×2)                                  | materializarlo es aditivo puro; decisión abierta: ¿scope jerárquico o match exacto? (A6 asume exacto); AX2 «desactivar el registro es un override» sin test                                                                                                                                                                                                                                          |
| Ex-PENDIENTE cr6/cr8 §1.9                            | datos VERDES (verbatim del espejo, guard 14/14) · estampado **ABIERTO**                                  | los 6 digests NO están en la tabla §15.3 (solo «IDs reservados»); **colisión de nomenclatura**: IDs reservados `-flujo@v1` vs archivos reales `-voltaje`; `metodos` de cr6/cr8 (`bruteforce+spectrum`) ≠ receta congelada §1.9 (CP-SAT+bruteforce) — la procedencia del ancla difiere; análisis de flips §15.5 no repetido                                                                           |

## 3 · Correcciones a premisas del backlog

1. **La visión producto projects/workspaces/chat SÍ está formalizada**:
   `docs/studio/product-model.md` (VIGENTE 2026-07-24) + decisión #78 — jerarquía
   Workspace⊃Project⊃Run, chat como superficie del project, rutas anidables. No quedó
   pendiente de sesión con Dylan; es la autoridad de M1/M15/M17.
2. **M14 definido**: es la raíz de composición del paquete Python de la imagen (la
   alternativa (b) evaluada y diferida en #95) — no un tema de Studio.
3. **La ICE-70 SÍ tiene mapeo bus↔subestación** (contra el comentario de
   `GridMap.tsx:18-23`): `corpus/ice-uniforme.json` trae `nodos` por NOMBRE de
   subestación del MISMO portal que los GeoJSON — con desajuste honesto 68/70 nodos y
   8 líneas descartadas (`edge_endpoint_names_resolved: passed=false`). M18 es menos
   ausente de lo enunciado.
4. **⟨C⟩ para M20 casi existe**: `blite.quantum.qaoa` ya expone `expected_energy`
   exacto por statevector (#21); lo que falta es evaluarlo en ángulos DADOS
   (`optimize: false`) y persistir los ángulos de Nexus con digest (hoy solo viven en
   el espejo externo).
5. **Los 13 entry points del registry incluyen 4 STUBs** (`graphs.partition`,
   `ml.classify`, `numeric.matrix_ops`, `smt.check_constraints`) — dos de ellos son
   exactamente los huecos naturales de los retos 2/3 (SVM precomputed, eigen/ED).

## 4 · Hallazgos NUEVOS sin ítem M asignado (candidatos a entrar en la consolidación)

| #   | Hallazgo                                                                                                                                                                                                                                        | Fuente                          |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- |
| N1  | `POST /runs/{id}/cancel` no existe — `run.cancelled` congelado sin emisor HTTP                                                                                                                                                                  | cobertura A + research producto |
| N2  | `approval.*` spec'eado con fixtures y CERO superficie (ni emisor en el loop, ni card en Studio, ni Zod espejo)                                                                                                                                  | cobertura A/D                   |
| N3  | `step_id` top-level en `verification.completed` (#70a) — la evidencia por paso llega `attestations: []`                                                                                                                                         | cobertura D                     |
| N4  | Artifacts/Knowledge de nivel proyecto + `deliverables=` nunca pasado a `assemble_bundle` (#70b) — honest-empty estructural en vivo                                                                                                              | cobertura D                     |
| N5  | Papers/upload: dropzone deshabilitada sin endpoint de archivos en ninguna spec                                                                                                                                                                  | cobertura D                     |
| N6  | **Gate de docs de CI ROTO** por el árbol vendorizado `knowledge/quantum/quantathon/…` (47 `.md`, 671 errores markdownlint + 48 prettier): cualquier PR que toque un `.md` falla `ci-gate`. Reproducido en vivo, **sin registrar en decisiones** | cobertura E                     |
| N7  | La regla de marca interna (el codename que jamás entra a texto del repo) NO sobrevive al flip OSS: su único enforcement es un hook local no versionado en la máquina de Dylan                                                                   | cobertura E                     |
| N8  | CI no corre en ramas de trabajo (`push` solo `main`); `ci-gate` no es required (GitHub Free); CODEOWNERS contradice la gobernanza #94                                                                                                           | cobertura E                     |
| N9  | Onboarding: `docker compose up` sin lista de servicios FALLA (worker inerte); `.env.example` incumple su promesa (omite `CHIMERA_MODEL_*`); no existe doc de USO para terceros (la autoridad 2 del criterio no tiene artefacto)                 | cobertura E                     |
| N10 | `install-dev.sh` escribe en `~/.claude/agents/` del usuario sin preguntar                                                                                                                                                                       | cobertura E                     |
| N11 | Sanitización pre-flip: el árbol quantathon vendorizado es material de terceros sin licencia declarada (mismo árbol de N6)                                                                                                                       | cobertura E                     |
| N12 | Divergencia de nombre nunca registrada: freeze usa `MODEL_ROUTER_BACKEND`, el código `CHIMERA_MODEL_BACKEND`                                                                                                                                    | cobertura A                     |
| N13 | ieee30: identidad tras la 2ª ancla sigue sin decidir (3 formas candidatas); la forma elegida aplica igual a cr6/cr8                                                                                                                             | cobertura C                     |

## 5 · Conflictos de contrato consolidados (gate de la Etapa 3)

Los que exigen decisión Dylan+Claude ANTES de consolidar; cada uno con su ceremonia:

| C#   | Conflicto                                                                                                                                                                                                | Ceremonia requerida                                                                                                                           |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| C-1  | **Freeze §13 contradice el código**: sigue afirmando «pipeline fijo Fase 1» + set hardcodeado mientras el loop agéntico corre — y `docs/specs/README.md` dice que una spec jamás contradice el freeze    | supersede [MEJORADO] explícito en §13 (o promoción de `harness-agentico.md` a autoridad co-igual) + catálogo §14 gana los eventos de #68      |
| C-2  | **Techo AL2 de `property_rule` vs Z3 `unsat`** (prueba formal valiendo AL2)                                                                                                                              | decisión de freeze: ruta `formal_exact`+proof vía cvc5/Alethe/Carcara (AL4, research R3) evita romper los 4 espejos del techo                 |
| C-3  | **Punto 4**: fail-loud de `GET /runs` vs skip honesto con `discarded_streams` — y el wire es array desnudo (envolverlo rompe E↔D)                                                                        | decisión #10x + extensión aditiva del wire + test con la píldora #96; línea roja: solo ruta de lectura                                        |
| C-4  | **M19**: `run.metrics.recorded` congelado con campos de confianza ≠ consumidor con campos científicos; además el evento queda FUERA del provenance_hash (¿la ciencia sin amparo del certificado?)        | supersede/extensión del payload §3 + decidir dos-brazos (sub-runs §13 califican) + `variant` a 4 valores (M6) en 4 espejos                    |
| C-5  | **M2**: `GatewayContext` congelado (2026-07-22) sin `run_id`/`step_id`; contradicción §13 «cada step cruza» ↔ `MediationStage` colapsa resolve+invoke; ciclo de imports runtime↔gateway obliga inyección | supersede aditiva del ctx con la MISMA ceremonia que lo congeló + decisión de granularidad del cruce + contrato `layers` nuevo                |
| C-6  | **M4**: `Verifier.verify()` retorna singular; N attestations por isla con `independence_group` distinto INFLARÍA las patas del punto 7 (el fraude que stress-final cerró)                                | extensión del puerto + regla explícita «islas de una corrida comparten grupo» + supersede de la diferición registrada                         |
| C-7  | **Rekor**: descartado ×2 en el registro; el fundamento (air-gap con Fulcio) ya no aplica a Rekor v2 posix + stapled proof                                                                                | re-litigar con causa registrada (research R3 §1)                                                                                              |
| C-8  | **M18**: convención de branch-ids inexistente en 3 modelos + regla de agregación per-isla del verdict inexistente (freeze §9 la exige «sin excepción»)                                                   | decisión de convención (opciones a/b/c en cobertura D) + regla de agregación + fixture de costura `contract/superficie/`                      |
| C-9  | **M20**: divergencia consumada del Studio vs `superficie-visual.md` §5 (AblationMetric[] sin eje p) — registrada pero sin supersede formal; el endpoint no existe en la spec E↔D                         | supersede de §5 + fila nueva en `endpoints-studio.md` + resolver la clave (por instancia vs por run)                                          |
| C-10 | **cr6/cr8**: IDs congelados `-flujo@v1` vs archivos `-voltaje`; `metodos` ≠ receta §1.9; digests sin estampar en §15.3                                                                                   | decisión de identidad (estampar `-voltaje@v1` con la variante declarada, jamás sobreescribir) — la misma forma que se elija para ieee30 (N13) |
| C-11 | **M9**: exportador OTLP es egress (debe vivir en `blite.protocols` + importar authz) pero su valor es exportar el veredicto de verificación — Inv-E/INV-6 lo prohíben                                    | decisión de arquitectura: proyector como proceso/consumer aparte fuera del engine (no toca los contratos)                                     |
| C-12 | **M13**: ADR-029 (manifests genéricos) vs tools MCP de terceros con vocabulario de dominio arbitrario                                                                                                    | decisión: exención declarada para manifests importados o re-etiquetado; + `DispatchStrategy` de red inexistente                               |
| C-13 | **M10**: dos disciplinas de digest (corpus `json.dumps` vs `C()` JCS del anexo) — publicar «el digest» sin declarar cuál rompe la verificabilidad por terceros; el de §15.3 es inmutable                 | declarar la dualidad en el export (no re-digestar); licencia/atribución del corpus ICE pendiente                                              |
| C-14 | **M7/C3**: `FormalExactPredicate` exige `CpSatStatus` que una ED no produce; no existe noción de tolerancia relativa (criterio oficial ≤5%) en ningún verificador                                        | extensión aditiva del predicate (status nuevo o hermano) + campo de tolerancia en `verifier_params_digest`                                    |
| C-15 | **M11/M20**: `baselines` del schema D5 cerrado a 3 claves (`cpsat/greedy/gw`) en schema+tipo+chart                                                                                                       | extensión aditiva coordinada en los espejos al añadir `sa`                                                                                    |

## 6 · Sustrato reutilizable confirmado (lo que NO hay que construir)

- Seam `Proposer` + ModelServer record/replay completo y verde (22+ tests); protocolo
  estricto `ProposedStep`; canonicalización única.
- `Pipeline` de 8 etapas fail-closed construido (solo falta cablearlo); `derive()`
  con atenuación garantizada; regex URN validada.
- `dsse.py` PAE sign/verify reutilizable tal cual para DSSE-por-attestation;
  `KeyProvider` Protocol con la escalera en el docstring.
- Importador Nexus 3-capas determinista + `ConsensusReplicationPredicate` con patas.
- Patrón provenance (`ExternalSourceProvenance`/`DerivationProvenance`) + capability
  ingesta genérica — sustrato directo de M10 y M13.
- 4 STUBs de capability registrados esperando implementación (`ml.classify`,
  `numeric.matrix_ops`, `smt.check_constraints`, `graphs.partition`).
- `expected_energy` exacto por statevector en QAOA (#21) — la mitad de M20.
- Componentes `assurance-ui` (badges/escala/veredictos) listos para M4/M18.
- El stack científico de los retos 2/3 ya instalado y probado vivo (sklearn, scipy,
  PennyLane, qiskit 2.5/aer) — smoke `test_scientific_stack_versions.py`.

# blite-cap-ml

Classical ML: classification, regression, clustering.

Scope note: this package is a stub. Its single entry point `blite.ml.classify`
(`Classifier`) raises `NotImplementedError` — no ML functionality is
implemented yet.

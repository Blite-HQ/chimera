# blite-cap-numeric

Numerical computation and data preprocessing utilities.

Scope note: this package is a stub. Its single entry point
`blite.numeric.matrix_ops` (`MatrixOps`) raises `NotImplementedError` — no
numerical functionality is implemented yet.

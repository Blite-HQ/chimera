# blite-cap-smt

SMT/formal verification using Z3.

Scope note: this package is a stub. Its single entry point
`blite.smt.check_constraints` (`ConstraintChecker`) raises
`NotImplementedError` — no Z3-backed checking is implemented yet.

---
track: Quantathon Bootcamp
session: b03
title: Introduction to Quantinuum's technology
speaker: Callum Macpherson
source_kind: drive
source_ref: 1JCCrnrT9SvS2mXi9q2rqPn6gKTaIz7Nk
content_type: transcript
language: English
---

# Introduction to Quantinuum's technology — Callum Macpherson

### [~00:00]

Okay, right now we're recording the session. Maybe, I think we can start. There's 20 members right now from the participants. Okay, sounds good. Yeah, so it's recording right now, so maybe you can start whenever you want. Great. Thank you. My name is Callum McPherson. I'm a software engineer at Continuum. I'm excited to be giving you an intro to our different software projects at Quantinium today. I'm looking forward to the hackathon. I will be attending in person with my colleague Agustin. So yeah, if you have any questions at any point, please interrupt and ask me. So I'm going to be giving you an overview of our different software projects, including GUPI, which is our programming language for quantum computing with this nice mascot. If you want to find out more about Guppy you can visit our website at guppilang.org. So just a brief introduction to Quantaneum as a company. So we are a global quantum computing company. We really work on like every aspect of quantum computing from like the hardware to the software algorithms theory quantum error correction really every aspect of the technology we're like a full stack company in that sense so we have i think eight sites across the world so i'm speaking to you today from uh cambridge in england which is here oh we have several offices in the us where most of the hardware is and most of the work on hardware is done. We also have an office in Japan and one in Munich also. So yeah like I said we're about, we really work on every aspect of quantum computing so error correction software, chemistry, algorithms and so on. So there's almost 700 people and yeah we're quite a research focused company. I'm going to be giving you a bit of an overview of our software stack today so I'll be talking about Guppy but also our Nexus cloud platform which you can access during the event. So yeah if I didn't say already please feel free to interrupt if you have any questions. So just before I go into any of the further details, I wanted to give you an overview of our hardware roadmap. So we are a company that's developing trapped ion quantum computing technology. This is the hardware modality we are using. So we have several generations of quantum computer, all based on trapped ions. So we have the H1 system, which was like a linear kind of array of qubits. It's 20 qubits in total. A couple of years ago, we released the H2 system, which was kind of a ring of 56 qubits. Our latest generation of device, which we've released, is a Quantinium Helios, which has 98 qubits in total. total. Also, I've been doing demonstrations of many logical qubits on a quantum helium. I'll link you to some of that research later if you're interested. But this is the point where we're on the roadmap at the moment where we're around 100 physical qubits. So yeah, like I said, in Quantenium Helios we have 98 fully connected physical qubits. So all fully connected in this context means that we can do multi-qubit gates between any pair of physical qubits. This, I guess, is in contrast to superconducting devices, where the connectivity is fixed because superconducting qubits are fixed nodes and interactions can often only be realized between neighboring nodes. So having these fully connected qubits really helps reduce circuit complexity and also allows us to explore a wide range of quantum error correction codes. Another thing that's really nice about ion traps as a technology is you have these really high fidelity two qubit gets So we now on like three ninths of two qubit gate fidelity So yeah this often is like the dominant source of error Typically, an order of magnitude worse than the single cubic gates you have in your server. If you're interested in reading more about this device, I would encourage you to look at this archive paper down here. Hopefully you can see the reference. Otherwise you can just Google Helios or Quentinium Helios and 98 qubits and you should arrive at the archive paper which will give you all of the details. So what does Helios look like? Well we have these 98 physical qubits and it's arranged in this kind of tuning fork shape where we have these ions arranged in a ring and in this sort of in these sort of legs of the trap we have these sort of gate zones there are eight of them in total and we have like these and this is where qubits are like paired up and where you sort of make the qubits interact together to perform logic Another thing I'll just shout out is that we are currently offering promotional access to H2, which is a previous generation of quantum hardware, the one with 56 qubits that's kind of in this ring shape. So we currently have this promotional campaign ongoing where you can submit a project with proposal and it will go through a review process and you may have the option to access some real quantum hardware for your application, which would be nice. So if that sounds like something that's interesting for you, I'd encourage you to scan this QR code or just look on our website. Any questions about what I've covered so far? I'm hearing some noise, but I didn't hear a question. Okay, if there is a question, please feel free to interrupt me. Unfortunately, I cannot see people raising their hands. I don't have the picture-in-picture view at the moment. Yeah, don't worry, you can continue. Nobody raised it. Okay. So now I'll talk about Guppy. And what is Guppy? Guppy is our quantum computing language, which we've developed internally, and it's really designed for like the next generation of quantum programs. So like the kinds of programs that we want to run as devices are getting larger and we're moving towards fault tolerance and quantum error correction. So there's really like, I guess, three points that are really worth bearing in mind with Guppy. So as you can see from this code snippet, it really looks a lot like Python. So it leverages the sort of intuitive syntax that Python provides. Python is, as I'm sure you know, a very widely used language within the quantum computing space. So it makes sense to leverage something that people are already familiar with. And it's also a fully fledged programming language as well. So it's not just a quantum computing library where you typically make a circuit representation, like a big list of gates, and then you list to the quantum computer, sort of like a low-level kind of assembly code way of building your program. Instead, Guppy allows you to really leverage the kind of abstractions that you'll see in like a normal classical programming language. So you can program with functions and loops, conditional logic and recursion. And this is all compiles down to something that can eventually be run on the real device. And in addition to the previous features, Guppy is really developed to focus on type safety. So it has a lot of built-in checks in its type system to catch implementation bugs and errors early. So if you're familiar with Rust in programming language, it's got a strict type system and ensures that you manage qubits resources correctly. So similarly Guppy has its own type system So if you allocate some qubits it ensures that you use those qubits for something and don just leak them and don make use of them And likewise you cannot once you measure a qubit you also can that qubit is deallocated or destroyed and you have to allocate another qubit. So these are built in safety checks, which are good for catching errors early, especially as programs grow in complexity. So we have a bunch of docs and tutorials at guppilang.org. The language itself is also fully open source, so you can view all the source codes on GitHub. It's distributed through a Python package, so it's embedded within Python, but it's distributed as this sort of package on PyPy. An additional point I would just shout out is that we're moving towards a V1, a version one stable release of Guppy very soon that has many new exciting features. So even though we won't be using Guppy version one for the hackathon, I'd encourage you to pay attention to all of the new stuff we're going to be releasing soon. Colum, someone raised their hand. Sawyer? Yes. Can we have this presentation by any chance? Yes, I think that should be fine. Would it be alright if I made a GitHub repository and shared the slides and also the code demo I'm going to present in a moment? And then I guess that link can be distributed. That would be awesome. Yes. Okay. If that works for you then, and the organizers, then that's fine. Hi, Colin. I have another question. So in order to use the quantum hardware, I mean, Guppy is the only problem in language or framework or whatever that we can use. I mean, I wonder, my question is more like, can we use Qiskit in some way via transpilation mode? or is the GUP really tied to your quantum hardware? That's a good question. I'll actually have a code example on this later. So one thing you can do if you're familiar with other sort of quantum libraries in the ecosystem is you can do like a kind of transpilation step where you translate from casket to another circuit representation. And then you can like make a GUP function definition from that circuit. So you can reuse some quantum circuit code that you have in other frameworks. But there are some trade-offs to explore there. But I'll have a code example later where I define a Guppy program from a Qiskit program. And so there's like an intermediate step you have to do, but it is possible. So if you have a lot of big unitary circuits lying around and you just want to see how it works with GUPY, then you can try that out for sure. Yeah, awesome. Yeah, thank you. Because I think what we learned on the initial course was Qiskit. So I was wondering about that. But yeah, no, thank you. Thanks so much. Yeah, no worries. Yeah. So just a bit of context on the previous slides. So I should probably explain like what this just looks like Python, right? But it's not quite Python. So Python is like, I guess, the host language for Guppy. So it's like a programming language embedded within Python. And we have this special like Guppy decorator. And this will really mark the kind of where the quantum program begins. So within this function, you can program with like native Guppy data structures like arrays and qubits and gates and so on. And those functions that you annotate with the special Guppy decorator, those are the things that get compiled down to the real device. so just I guess I've covered some of this already but I just want to stress that this is a bit of a departure from the kind of static circuit representation that you might be familiar with so typically quantum programs are often specified at like quite a low level of abstraction almost like kind of assembly code where you're essentially building a circuit by sort of appending gates one after the other. So in Guppy we're moving to a much more I guess dynamical model of computation where instead of just building a static list of gates the quantum computer is somehow in constant communication with a classical coprocessor So basically we have all of these new device capabilities that our hardware supports and we want a language that allows us to leverage those capabilities. So we have the ability, for instance, to dynamically allocate qubits and do mid-circuit measurement and do real-time branching and looping and so on. So we really want a language that can express those things. Yeah. This also has like kind of a slightly counterintuitive effect. So like it will mean that in principle, the number of qubits that your program uses can be different on different device runs because you can do things like allocate a fresh qubit depending on the outcome of measurement and so on. So it's really this constant back and forth between classical and quantum computing, I think, is key to understanding GUPI. And these sort of unlock a whole new set of applications in quantum error correction and algorithms and so on. Calum, there's a question. Diego says, does Guppy has any function to simulate the quantum computer on our own computer, like Qiskit that has the AER, or it was only created to be used in real quantum computers? Yes, I will have some examples on that later. But yes, we have a built in like framework slash simulator for executing Guppy programs. And this, I think, is very important for debugging and testing purposes, because you don't want the first test of your quantum program to be on the device. You want to simulate it to make sure it's behaving as you expect. And once you're confident in your implementation, it makes sense for you to submit to the device. So we have our own simulator called Selene, which has a range of simulation capabilities. I'll show that a bit later on. Okay, thank you. Just one thing and I'll get on to a few more slides and then I'll get on to the code example. So another built-in feature of GUPPI is this idea of type safety and sort of these additional features for ensuring the correctness of your program. So Guppy is like a strongly and strictly typed programming language. So if you've used, like I said before, like Rust or something, you sort of compile your program and then you get like a compilation artifact. And then that gives you like something that you can execute after that. And there's this type checking process that occurs. And we have like built in ways to check that you're using Qubit resources correctly. We had Hank and Phil. They're great. I really like those guys. We asked them what their objectives were and, like, why they... Because we... Brittany over-programmed the meeting or over-and-under-programmed it at the same time. Sorry, is Sagar doing, like, a question or your microphone is open? So we asked, like, what... Didn't... I'm starting to ask a question. Yeah, it wasn't a question. He just had the open microphone. Oh, okay. Yes, no worries. Yeah, so basically, this will become clear in the code examples, but we have some built in like checking for correctness that can catch certain errors at compile time before you simulate or execute the program on a real device, because it's good to catch errors and mistakes earlier rather than later. Yeah, so I just have a couple slides on Nexus, which is our cloud platform for what to be seen. So the relevant parts of Nexus for this hackathon are we have this JupyterHub environment, which has all of the packages pre-installed, including Guppy and Qiskit and all of the familiar packages so in theory you shouldn't have to worry too much about installing things locally the organizers can add you to a nexus organization and you should get an email and once you have the email you can click on the link and you can make an account and then you can just log into this website and you should be able to just open up a Jupyter Notebook in a web hosted environment and and just play around straight away. So there's no, you don't need to worry about installing anything locally on your machine unless you want to, that's also an option. But yeah, it's nice to have all of the packages pre-installed. So yeah.

### [~20:00]

Yeah, GUPY is where you do your patient programming. And then Nexus manages all of the jobs and allows you to sort of monitor pending jobs and view like you're like there are also simulators hosted in Nexus as well. So we have this lean simulator, which I spoke about before. You can also access Quantinure Helios and H2. H2. So I should say for the hackathon, we'll only be using the simulators, Celine, and also the H2 simulator. But if you are interested in device access, we do have this promotional campaign on at the moment, which you can apply for. So in addition to... I have a question. I'm sorry, can you repeat the question? Do you hear me? yeah yeah talk uh well i have a question that is that is any of these quantum computers has like a hpc implementation or like parallel computing implementation uh that's a good question um we do have some stuff that's public on um gpu integration with helios as far as like HPC, that's an interesting direction. Yeah, I'm not sure we have anything that we've released on HPC specifically, but if anything occurs to me, I'll link it to you. But yeah, we do have some co-located classical hardware. I'm not sure about HPC specifically. Thank you. So yeah, I'll just go through this quickly. So Nexus has this interface where you can see all of your previous jobs. So the jobs where you sort of execute your quantum circuits and compile them and so on. So you have like a log of all of the previous jobs. You can also kind of share code with one another, share results and so on. And the most relevant feature really for this hackathon is this pre-built Jupyter Hub environment. So like I said before, you can get the invitation email and you can just log into this platform and start programming straight away. You don't have to install anything locally or worry about dependencies and so on. and so on. There's also some visualization for like tracking the amount of simulator time you're using and so on, and some organization level things about quarters and so on, but those aren't so relevant for the hackathon. Okay, that's the end of the slides. I'll take any questions at this point, but if there are no questions, I'll get started with the code demo. Can everyone see my Jupyter notebook? Yeah, we can see it. I have a question in terms of, I mean, let's say that I'm going to trigger a job, right? A quantum job or whatever. Is there like a really big queue in terms of processing? I mean, do I have to wait like one hour for execution later? or is it going to be faster? For the purposes of this hackathon, hopefully not. We can allocate some simulation resources for this event, so hopefully you won't get stuck in the queue. If there is a lot of people running stuff simultaneously, you might notice, but hopefully not. Of course, if you're getting to the real hardware, that's in high demand, so that's got a longer queue. but makes sense thank you thank you so much all right so yeah i'm going to start with this super basic example so i think this will be revision for a lot of people i guess just a simple program to prepare and measure the bell state so what's the bell state the bell state is exactly an equal superposition of this zero zero and one one basis states where zero zero and 1,1 are these vectors here that live in Hilbert space. So as I'm sure you know you can compare this bell state with just a single Hadamard followed by a CNOT gate and then we can apply some measurements here. So the Hadamard has the effect of creating a uniform superposition of 0 and 1 and there a relative phase if you start in the 1 state And then CNOT is just an an entangling to a cubic gate that flips the target bit if the control bit is set So it maps 1 0 to 1 1 and 1 1 to 1 0 and acts as the identity on anything else. So what does this look like in Guppy? One second, I'll just scroll back up. I can define this function called prepareBell, which takes no inputs and returns an array of qubits. So the first thing I do is I have to allocate some qubits to use. I then apply a Hadamard and a CNOT, and I can return this array of two qubits here. You will see I have this check method here. This kind of ensures that I haven't made any kind of obvious mistakes. This is like the type checker ensuring that I've managed all my qubit resources correctly and the types of my variables make sense. So if I make a mistake here, like I apply this C0 to this same qubit, I'll get the compiler will shout at me and say I've made a mistake. So we fix that. So super trivial program here. We just want to call this function to prepare the bell state. That will give us this queues, which is an array of qubits. And then I can just sort of log the results of measuring those qubits with this kind of results function here. Now, this is where we're doing the simulation. So now we have our very basic program. We can now execute that program using a classical simulator. And because we're just measuring a uniform position of 0 and 1, we obtain 0, 0 or 1, 1 with equal probability. equal probability. So we can execute this program for like 1000 shots. And let's see. So we can get this counter object and we can plot our results. And we see that we end up with roughly equal number of shots landing in the 00 and 11 basis state. So not exactly 50 50, but we're essentially doing a coin flip on every shot. So I would expect roughly a 50-50 distribution. We can also generalize this just to n qubits. So this construction just generalizes to so-called G at Z states. So this is just an n-fold tensor product of 0 and 1. So we can just use a for loop here and just apply the same initial head amount and then just a chain of C knots. So, yeah, I noticed that we've like kind of generalize, made this function like generic over this type variable n. So we've generalized the construction for n qubits. And we can type check that again. We define this main function, which is like an entry point to the program. So this is just the function in which we call the other functions. We allocate the qubits. We do the prepare G8s subroutine. And then we measure those qubits. Let's simulate for a thousand shots again. We have to pick in concrete number of qubits here because yeah we have to choose a value for n for our program to make sense chosen four. So we see we get this approximately 50 50 distribution of the all zero and all one state. Now I want to give a brief interlude on using before I cover a more interesting example I'm going to show you how you you can kind of reuse some basic Qiskit codes that you've defined previously. So I've defined just another trivial quantum circuit to just prepare the Bell state. I've defined it in Qiskit this time. The work around for importing this into Guppy as we first convert it to PyTicket, which is our kind of previous generation quantum computing library. It's sort of similar in scope to Qiskit. It works with like a circuit-based framework. And you can see by using this sort of built-in conversion, this Qiskit to ticket function, I can obtain an equivalent circuit to the Qiskit circuit. So it's just like a handy one-line conversion. I can now like use the circuit to define a guppy function. So I can use this handy guppy.load I take it and then pass in the circuit And then just call this circuit as like a subroutine in my Bell State program So you can see this works just as well for, in this case, for doing the Bell experiment. So obviously this is a very simple, trivial case, but this could, in principle, work for more complicated circuits as well. I would say that there are certain advantages to programming things in GUPY from scratch, but if you have some code that you just want to try to reuse, you can do so. Let's look at a slightly more interesting program. So you may have seen a circuit like this before where we're attempting to apply this T operation to our quantum state. And the way we do this is we prepare this T state, which is like this state here. So we have this equal superposition of 0 and 1, but we have this pi over 4 relative phase, this non-Clyfford phase. And in terms of this circuit appears quite a lot in quantum error correction for like injecting non-Clyfford kits. I could say more about that later, but I'm just using it as an example so far. So I can implement this program here by allocating a qubit. I then apply a Hadamard followed by a T to prepare the T state. This is just doing what this equation says here. I can then like branch of this measurement result. So I can perform a measurement on this qubit. And then if the measurement is zero, I can do this. I can do, sorry, if the measurement is zero, I'll do nothing. If the measurement is one, I will apply this S times X operation. So you can see I just apply this with native Python syntax. So this is a very simplified case, but you can see, you can just do conditional logic with the syntax you're used to in Python, which is very nice in my opinion. So this is just an idea for an exercise if you have time. So it turns out you can prepare the GHZ state in constant depth if you kind of leverage these mid-circuit measurements and so on. So you could like construct a program to prepare this GHZ state, which we discussed before, using n minus one additional qubits by using this kind of extra conditional logic here. So if you have some time and are keen, I can link you to this notebook and then you can give that a try. Yeah, I'm doing okay on time, I think. I want to discuss briefly some of the ownership rules which Guppy imposes. These are some things that help ensure the correctness of your program and sort of catch certain errors at compile time before they hit a simulator or a real device. Ideally, we want to catch errors as soon as possible because quantum computers are an expensive resource. We don't want our jobs failing because we made a mistake in our implementation. So I've made a deliberate mistake here. The mistake here is that I've allocated a qubit. I've allocated some quantum bit. And then I've done something to the bit, but the way GUPY works is you need to either deallocate to the qubit with like a measurement or a discard or return it as an output of this function here. So one way to correct this would be just to return the value Q1. So we get this qubit back after we've done it. And that way the qubit's not leaked, we can still access this qubit in other parts of the program. This first case indicates an implementation bug which the type system can catch early. I'll show one more example before I show you a less contrived example. So, yes, I have this simple function here that implements the Hadamard test. and this just encodes the expectation value of this y operator into the probabilities of measuring 0 and 1 on this qubit. So if I look at my statistics, I can figure out what this expectation value of y is. And I've allocated a qubit and applied some gates, but I forgot to add my measurement here. So now Guppy will see that I just left this qubit idling and not measured So I need to fix this by applying the measurements and so on so you can see I can catch these kind of mistakes or bugs in my program yeah okay yeah right Yeah, so another mistake you can make is applying a gate to a qubit after you've measured it. So qubits need to be explicitly reinitialized. So measurements deallocate the qubit, they deallocate the bits, and you need to reinitialize them before you're allowed to use them in your program. So if you try and access your qubit to apply another quantum operation, that qubit, you're pointing to a qubit that has been t-allocated, so that's a mistake. I'm going to skip ahead a bit here. So I'm just going to show you a simple program to prepare a graph state. If you don't know what a graph state is, it's essentially we apply hadamards to all of our qubits and then we apply a bunch of CZ gates and which CZ gates do we apply? We'll apply CZ gates according to the connectivity of this graph. So we'll do a CZ between qubits 0 and 1 and then another one between 1 and 2 and another one between 0 and 2. Why is this an interesting example? It's because here's an example where we're kind of loading in a value from outside in Python. So you notice that, like, I can take in a graph here and I can access the edges of that graph. And then I can load this in as a comp time value. So I can load data in externally from Python. provided it's of the right office set of allowed types and then this data from python can inform like how I write my quantum program. Colum, Benjamin raised his hand. Oh yes. Hello. Hi, can you hear me? Yes. Okay, I have a question. Specifically related to the pendulum language. You have already mentioned that there is like a way to transform this circuit, the Qiskit to Gopilank. But I have a question. Is there a similar way to transform Penny Lane to Gopilank? I think the translation from Penny Lane is probably less straightforward, to be honest. On this translation point here, I'm essentially using the handy conversion function from our Qiskit extension. So we have an extension specifically for integration with Qiskit. We do have an old extension for integration with Pennylane, but it is not currently maintained, unfortunately. So it will be unfortunately harder to convert your program from PennyLin to Guppy. But yeah, if you have a specific example that you need to help with, then I'm happy to help out with that. Yeah, that's just a question because I'm more used to work with PennyLin, but thank you. Yeah. Yeah, OK. I guess you can... it's going to be crazy, but you can translate from PennyLink to Qiskit and then from Qiskit to... You can send your program on a long adventure and hopefully... Yes, yes, yes. You can also, if you have a way to export to Chasm, that's quite a good way from getting other languages as well. But yeah, if there is a conversion to Qiskit or Chasm, if you can get to that point, then we can get the rest of the way, if that makes sense. So all of the examples I've shown so far have been like super simple. They've just been like essentially bell states and ghz states and very easy stuff. So I want to show something a bit more advanced, which shows like some additional features of GUPY as a language, but also some additional features of our hardware. So what am I doing here? How many people are familiar with this way of referring to quantum error correction code?

### [~40:00]

So this notation here means that I have seven physical qubits and I'm using those seven physical qubits to encode one logical qubit. And then there's this extra parameter distance, which relates to the number of errors you can correct with the code. That's not so relevant here. So basically I want to prepare a quantum state which encodes the zero logical state in the STEAM code. So STEAM code is a way of encoding logical qubits and physical qubits. So the logical equivalent of the zero state is actually this big complicated physical state on seven qubits. So this is more complicated. So basically I want to encode this state and prepare it and then afterwards do some programming on some operations on the logical level, hopefully. So the way I can do it is this. So I can attempt to prepare the logical zero states. I then check if any errors have occurred during the preparation of the state. So I can apply some gates and if any of those gates cause an error, then I can measure some extra and silly qubits. And if those extra and silly qubits indicate a measure like an error has occurred, then I can early exit on my program. So I can exit the program early without finishing it and go on to the next shot. So. Let's proceed with the example. So I have this struct, which is kind of like a class or a data class in Python. So I've made this steam qubit. And what is a steam qubit? This steam qubit is just an array of seven physical qubits. And these seven physical qubits comprise a single logical qubit. Now I want to prepare the sequence to prepare the zero logical state. So I can essentially allocate some qubits in Guppy, apply some hadamards on the indices 0, 4, and 6, and then apply a bunch of CNOTs according to this configuration here, and then return an instance of my struct with those data qubits. Now if I want to do the kind of post selection or early exit I can import this kind of exit function and I'm allocating an additional ancilla cubic here and then I'm essentially doing some extra two cubic gates to interact these seven physical qubits with my ancilla. I can then measure this encilla to see if an error has occurred. And if an error has occurred, then I can just exit the program. But if no error has occurred, I can just return my Steam qubit without errors. So now one thing I can do is I can run a simulation to check that this is like actually detecting errors and exiting the program early. So I can define an error model of the polarizing noise. So I've defined some parameters here. Not a very realistic noise model, just some single and two-qubit gate errors. And I can run this program. It's got eight qubits in total, seven qubits for the physical qubits of the code, and one in silver qubit. So I can execute this for 1,000 shots with this custom error model and see what I get. I can also do some like diagnosis so I can measure all of the qubits and then see how many errors actually occurred. I forgot to execute this earlier cell. My bad. So yeah I'm now running and I can see this got this progress bar as my simulation is going. It's quite an easy simulation so it went very quickly but um yeah um so i can get some statistics so i've done a thousand shots and 968 of them um no error happened um 23 of those um 1000 uh shots um an error happened and i exited the shots early and just went on to the next shot so i've like discontinued my program and and on nine of those 1000 shots, an error has occurred, but I've not been able to detect that error. So I like essentially let those errors through if that makes sense Yeah So here another exercise which you can do if you have time Yeah. I would say that, just a reminder that we have a bunch of docs and tutorials on the Guppy website. And also we have an open source GitHub repository where you can contribute if you're interested. And here are all the references. I'll leave the QR codes on screen for a bit. Are there any questions at this stage? I do have some additional content I can show you. I think there's still a bit of time. Yeah, we have time. Maurizio has a question. Okay. Hi, Callum. Can you hear me? Yes. So thank you for the very nice introduction to Guppy and Nexus and all of these things. So the participants during the hackathon next week, they'll be running their quantum circuits in the emulator, and they could do a noiseless simulation or a noisy simulation. So what would be the most realistic noise model that they could use? they say if they wanted to simulate the most accurately way possible the the real hardware of continuum what would that noise model be and how can they call it uh yeah that's a good question I think it was. Yes, it was. It broke my mind with that question, Mauricio. I started quantum hardware. Yes, I was going to talk about it. I think it was like 0.03 in channel D, that's the standard standard for the ion atrapados. But it's not only the polarization, because I think the model of the sound is asimetric. I mean, it can't be a deep polarizing channel, it has to be with different rates for Z and X, I think. But let's see. And more, different rates for two-qubit gates than for single-qubit gates. So, I suppose that they have a RIO already established. Hello. Hello, we missed you. very sorry my browser crashed for some reason I can I can yes you were asking about the noise model oh I have something on that if I just can reopen my window yeah one second so share my screen hopefully this works Sorry, I'm having some issues with it. It's funny because you are just sharing what it says. You are just sharing your entire screen. Remember to choose the window. Yeah, okay. I'll do it again and see if I can get it right this time. Share. Yeah, okay. This should be better, hopefully. Can you see this browser window? Yeah. Okay, so this is, I guess, what it will look like when you guys log into Nexus. Hopefully it will load. I don't know if my browser is having difficulties today. Yeah, not sure why it crashed out of nowhere. But yeah, I'm using this kind of built-in JupyterHub with the pre-built environment. So just to answer the question quickly, essentially you'll be able to log in. You'll create a project corresponding to like this hackathon or whatever you're doing. So this is just a way for Nexus to log the specific task that you're doing. Then I can write some guppy, this is just like a test main function. I can then upload that to Nexus. So I'm like taking this program, compiling it and then uploading it to the Nexus platform I then have like all of this configuration I can do so I can configure this CELINE kind of model This is just a basic state vector simulation it noiseless But there is also the stabilizer simulator and there is a noisy, again, there's a depolarizing our model and there's also the advanced Selene Plus. So this has a more realistic noise model, which attempts to capture the noise that would be present in the real Helios device. So that's based off like a physics informed error model. So you can like pass some custom parameters to here as well. So yeah, hopefully this should give you a way to run a more realistic simulation. you can run the simulation with the real noise model. Does that answer your question? Yes, it does. Thank you. Are there any additional questions? Sorry, I think I connect later to the meeting. So Helios is your quantum hardware, right? The real quantum hardware, right? Yes. So Helios is our real quantum device. We have 98 physical qubits. Yeah, we're not able to access Helios for the duration of this hackathon, but we will be able to give you access to a cloud-hosted simulator with the Helios noise model. So you're taking the noise model from the real one in order to the emulation, right? Yes, exactly. So this Selene simulator, you can run it like locally. So you can do a totally like the notebook I showed you previously. That was all just me running something locally. But there is also this fancier instance of Selene, which is deployed to Nexus. And using this Nexus version of Selene on the cloud, you can use the real error model of the device. Awesome. Thank you. You can also do like these extra simulators with, I'm pretty sure you can access this one for the hackathon. You can do like matrix product state simulation with tensed networks and some other things. And stabilizer simulation, which I think I mentioned earlier, which allows you to simulate much bigger circuits provided that they are Clifford only. So if you have only Clifford gates, then you can run big simulations if you wish. So, Callum, on that note, this second simulator that you mentioned, the tensor network based. Yeah. That's for which kind of circuits or can it simulate even non-Clifford circuits? Yes, it can simulate non-Clifford circuits. I'm not an expert on tensor networks but the simulation will be more bounded by the bond dimension or the connectivity of your circuits so if you have a lot of long range entanglement in your circuit then the tensor network simulator will start to struggle but if you have only local entanglement, local two cubic gates and a bunch of non-Clifford gates then that should still work. I think there is an example here possibly. And they could go beyond the 26 qubits that the exact simulator can simulate, I would assume, right? With this internet one? I believe that's the case here. I'm not sure if there is a built-in upper limit. I'm sure there is somewhere, but I can check what that is for you. Okay, thanks. Sawyer, you had a question? Yes. So a lot of times in other resources for computers, you have different ways of over-allocating, store stuff and things. Is there a way to, I guess, over-allocate qubits? Have more qubits allocated than they're physically available in some form of swapping or something like that? Yes. I mean, there are some built in checks in the type system to make sure you don't allocate qubits that you don't use. If you allocate a qubit, which you don't measure or don't return to like outer scopes or another piece of your program can access it. That is a type error and Guppy will not let you compile the program. I'm trying to think if there are other instances. Obviously there is like a hard limit on 28 qubits in total for any given point of the execution on Helios but it is possible that qubits can be reused So maybe you finished using some qubits for one part of your computation and want to reset them and then use them later And this is this is possible. So at any given point in your computation, you can use a maximum of 98 qubits, but you will be restricted. Sorry, you can in principle reset your qubits and then use them, reuse them again. But at any given point, you can't go beyond the capacity of the device. So this, yeah, so the reusing has to be done manually. Like, so if I need more than 98, I need to store the values and load them again. Yeah, you can, there's, I can maybe like a small example on that if you're interested. Yeah, I did have another example I was going to cover. I think I only have 15 minutes left though. Is that right? Yeah, but we can extend it a little bit. Okay. Yeah, okay. Let's see. Yes. Sorry, my browser just crashed, so I've lost some of my... If I open VS Code, can you see VS Code? Can you see my code editor now? It's not appearing on the screen, I think. Oh, okay. Let me try that again. Yeah, because you're just sharing the window. Yeah, okay. I got it. Yep, that's right. So let me just change. I mean, this is probably a longer example, but I'll see how far I can get through it. Let's see now. If I share, it's still gone. I'm doing the same thing as I was doing before. Not sure why this is happening. Try again. Let me share this window. Sharing the entire screen. How do I share the window? I'm sorry, this is probably not very interesting to watch. Don't worry. Yeah, I thought it was again. It seems to be a bit random. Try again. I think if you. Oh, yes. Can you see my code now in this notebook? Or do you still see something? see the meetings then. Oh, OK. You should be hopefully seeing a example notebook that I have in VS code. If I switch windows again, you see the meeting there, right? You can try like sharing the entire screen instead of a single window, then we would be able to go to the next. It notifies me that I am sharing my entire screen, but it seems like you can't see my screen. Is that correct? You just see the browser window? Yeah. Okay. I don't know why this is happening, sorry. It could be a permissions thing. At the top left of your search bar, there might be a tab to manage permissions. Yeah, I've already set the permissions. Let me try one more time. Sharing the entire screen. How about now? Do you just see like a tab or do you see like this browser notebook again? Now we just see the little screen. You sharing your entire screen. Yeah, I think I've had this previously where it like I'm using the Firefox browser and sometimes it doesn't work properly with Google Meets. So I can try one more time. And if that doesn't work, then maybe I'll have to show you during the event itself. What do you see now? The same, the browser. Okay. Yeah, maybe I'll just show this. Do you see GitHub now? Yeah. Yeah. Okay, so I'm not going to be able to show you the notebook for some reason, but I can show you just in like the source on GitHub. I can show you an example of solving the max cut problem with

### [~60:00]

which you might find interesting. It's like a NESC algorithm for optimization. So I guess a lot of GUPPI is geared towards like more fault tolerant kind of QAOA programs, but you can also do all of the other programs that you're familiar with. So MaxCut is like a famous kind of optimization problem that you can solve with QAOA, which is the quantum approximate optimization algorithm. So what is this problem, first of all? So basically, in MaxCut, you're given a graph, and you want to basically partition the nodes of the graph into two sets. And you want to do this partition such that the number of nodes of different colors or different labels are maximized. So this is a very simple graph here. but basically if I had a larger graph I'd want to maximize the number of blue nodes connected to red nodes and I can like encapsulate this problem in like a cost function which I can then try to to maximize this if I give these colorings like boolean values of like plus one and minus one then I can capture this cost as just this sum over all of the edges, one minus the product of the plus and minus one. So I can actually map this cost function into a Hamiltonian, and then I can have this kind of variational algorithm for trying to solve the max cut problem. So I'm sorry I can't execute this notebook locally, but I can scroll through it and then hopefully you'll get something from that. Benjamin has a question. Yes. It's a good question. Are we going to have availability to all those notebooks because they're so interesting? Yes. I think what I should be able to do is just create a GitHub repository and then I can just dump all of it in the repository and I can get the organizers to send you a link to that repository, if that would work. Yeah, because it's like an extremely interesting manual of using Guppy and all that, so thank you. Yeah, okay, cool. I'd also recommend you look at the documentation on guppielang.org. There are a bunch of pre-built examples you can look at there. So, yeah, I'm not going to spend too much time going through all of the theory here, but basically I have these in the QAOA, I essentially have these two Hamiltonians, which are like evolve for different times. and then I can kind of alternate between those Hamiltonians and then I can explore my solution space somehow and I will get if I run this on a quantum computer I will hopefully gain some information about the solution to max cut problem so I have this cost Hamiltonian which defines This is exactly just mapping this cost, this classical cost function into like a quantum, like a Hamiltonian form. And I using instead of just these binary variables I using the powerly z operator which can like sort of distinguish between the zero and one basis states I then have this like mixer Hamiltonian which is just essentially like an X operator applied to all of the qubits This is just to essentially make sure I explore like all of my solution space. If I'm just doing like rotations in the Z direction, then there's a lot of my solution space that I'm missing out on. So what do these Hamiltonians look like? Well, for this very simple graph, they just look like this. So I essentially have this identity term and then I have these ZI terms, which like somehow encode the connectivity of the graph. And then I have this mixer layer, which is just X applied to all of the qubits individually. Yes. So how do I do this in Guppy now? Well, it turns out, I mean, there's some theory I'm glossing over here, but it turns out you can implement the cost of mixer layers. by alternating these parameterized two qubit gates in the z basis and these single qubit gates in the x basis. So I can like build a python function that essentially constructs an instance of QAOA for some parameters. So essentially the optimization problem here is I want to figure out the times for which I'm evolving my Hamiltonian. You see that there are these parameters, these gammas and betas, and these are the parameters I want to optimize to try and find a good solution to the problem. I can build a single kind of instance of the problem which just takes the parameters as input. I just implemented it as like a guppy array of floats. So I'm essentially taking in the graph, the edges of the graph, and then the number of nodes in the graph determines my number of qubits. And then I apply Hadamard to all of the qubits, apply the Z rotations according to the connectivity of the graph, and then the X rotations on every qubit. So after I've done that, I can then have some way of calculating the expectation values of the. Calculating the expectation value of the Hamiltonian, it's just kind of like an energy or a pseudo energy. And QA way, essentially, I want to try and maximize this expectation value. So I have this build QAOA program function, which essentially gives me the main function I need as the entry point to my GUPY program. So this main function takes the cost and mix wrangles as arguments, does the QAOA instance and then does some measurements there. and so then I want to just simulate this for a number of shots and get my my result so I've got some post-processing here to calculate the energies and so on but I'm not going through that in detail so now I just want to do my variational loop so I can define an objective function which essentially is the aim is to sort of maximize or like I'm using the minimize function here but I've used the minus sign in front. So it's essentially maximizing the return value of this objective. Tell them a question We only have access to Nexus right We won have access to in quantum No that not planned for this event Are you particularly interested in quantum chemistry applications Jose Luis. We abrir the mic también. It was a participant's question, not mine. So, I mean, I could ask you that that would be interesting for you. But so far, we'd we'd not plan to use in quantum. I'm not an expert in quantum, so I might need some help. OK. Yeah. So basically, I've run through all of the theory here, but now we want to solve an instance of this max problem for like a simple graph. So let's say our graph for which we want to solve MaxCut looks something like this. And for this particular graph, we can actually just see the solution by hand. So we can easily verify that our quantum computation is on the right track. So the solution to this graph is just to do an alternate colouring here. So if I colour this red and this red, this one will be blue, this one will be red, this one will be blue, this one will be red, this one will be red. So it's kind of easy to see that that's the solution that maximizes the number of blue nodes connected to red nodes. So there are actually two like somehow equivalent colorings that are just like the inverse of one another. So we expect to get basically these are the two valid answers to MaxCut here. So now the Hamiltonian, the problem Hamiltonian for this graph looks something like this. We have this identity term and then we encode all of the edges by these Z operators. And this is the Hamiltonian, which will then evolve over time in our QA way. So now we can run this for 2000 shots. And we also want to do this kind of, as you did explain this before, we did this variational loop where we have some parameterized circuit. and then we want to find the parameters that which maximize the energy or the expectation value of for our QAOA problem. So we essentially do like a loop of calculating 100 different expectation values and we're trying to find the parameters which maximize those values. So you can see we've done 100 iterations of the loop and we get these parameters here and these are the parameters which like optimize our energy value. So we can actually plot these here and we can see that the most frequent outcomes in this kind of shot distribution are exactly those which correspond to like the optimal coloring of the graph. You can see that these are just like the binary inverse of each So with these two green bars, these are just like zeros and ones swapped. So these two green bars encode the correct answer to the optimization problem. And all of these other shots correspond to things that are not the optimal partition of the graph. And yeah, I can translate these like bit strings into colorings. And you can see that I have these kind of alternating colorings which correspond to the optimal solutions for this very simplified graph. This was just an illustration so that you can get some intuition that the program is working But yeah that doing QA away in Guppy Are there any questions about that Sorry, I realized I went through that pretty quickly there, but I can send you the link to this example if you want to work through it slowly. Yeah, I think there wasn't any questions. Yeah, thank you. Okay. Yeah, no worries. Okay. I think that's everything I had to present. If there are any further questions, I'm happy to answer them. But otherwise, I guess I will send them in Costa Rica. Thank you very much. Thank you. Thank you. So if we have like any doubts during the hackathon into any implementation, we can ask you there. Yes, you can ask myself and my colleague Agustin will also be there to answer questions. Thank you. Any more questions, anybody? If not, then thank you, Callum, for your time. It was great. I feel like everybody got a little bit of a taste of Guppy and your softwares. Very thankful for the partnership as well with Continuum. And we'll see you here next week. Very excited. Thank you, everyone. See you. Also, for all the participants, this will be recorded. It's already recorded and we'll be going to be on the media where we've been on the other classes. Good to see you next week. Thank you guys for leaving there. See you. Okay. Thank you, Callum. Bye. Bye. Thank you. Thank you.